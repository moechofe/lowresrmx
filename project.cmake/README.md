## Command line (tested on Ubuntu 24.04.4 LTS)

Tested with SDL3-3-4.4 in `project.cmake/SDL3`

Follow `SDL3/docs/README-linux.md`

I also install `libimecore-dev libzhuyin-dev libimepinyin-dev`, I don't know each one fix the linking issue with `SDL_ime.c`
